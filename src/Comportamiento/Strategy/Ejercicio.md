Implementaremos el patrón Strategy para manejar diferentes métodos de pago en una aplicación de comercio electrónico, permitiendo que el cliente elija el método deseado en la caja.

Crear un sistema donde el Contexto (`CarritoDeCompras`) pueda cambiar su forma de procesar el pago (`tarjeta`, `paypal`, `bitcoin`) en tiempo de ejecución.